class Solution:
    def arithmeticTriplets(self, nums: List[int], diff: int) -> int:
        count=0
        for k in nums:
            if k+diff in nums and k+2*diff in nums:count+=1
        return count
        
